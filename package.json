{
  "name": "ed-games",
  "private": true,
  "packageManager": "bun@1.1.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run -C packages/domain build && bun run -C packages/app build && bun run -C apps/api build && bun run -C apps/web build",
    "dev:api": "bun run -C apps/api dev",
    "dev:web": "bun run -C apps/web dev",
    "clean": "bun run -C packages/domain clean && bun run -C packages/app clean && bun run -C apps/api clean && bun run -C apps/web clean"
  }
}
